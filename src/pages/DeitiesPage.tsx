import Deities from '../components/Deities'

export default function DeitiesPage() {
    return (
        <div style={{ paddingTop: '88px', minHeight: '100vh', background: 'var(--ink)' }}>
            <Deities />
        </div>
    )
}
